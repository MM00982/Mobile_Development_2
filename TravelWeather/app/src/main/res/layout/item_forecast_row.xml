<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:background="@drawable/bg_white_card"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:layout_marginBottom="10dp">

    <!-- День недели (Фиксированная ширина, чтобы колонка была ровной) -->
    <TextView
        android:id="@+id/tvDay"
        android:text="ПН"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_width="50dp"
        android:layout_height="wrap_content"/>

    <!-- ЦЕНТРАЛЬНЫЙ БЛОК (Иконка + Описание) -->
    <!-- Занимает все свободное место (weight=1) и центрирует содержимое -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/ivForecastIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_rain"
            android:layout_marginEnd="8dp"/>

        <TextView
            android:id="@+id/tvDesc"
            android:text="Ясно"
            android:textColor="@color/text_secondary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>

    <!-- Температура (Фиксированная ширина, прижата вправо) -->
    <TextView
        android:id="@+id/tvTempSmall"
        android:text="20°"
        android:textStyle="bold"
        android:gravity="end"
        android:textColor="@color/black"
        android:layout_width="40dp"
        android:layout_height="wrap_content"/>

</LinearLayout>